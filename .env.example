# Exact public address, no path. Use your production Vercel URL after deploying.
APP_ORIGIN=http://localhost:3000
# Use your hosted PostgreSQL provider's pooled TLS connection string.
# This value is server-only. Never prefix it with NEXT_PUBLIC_.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
